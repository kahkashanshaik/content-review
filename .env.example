# Local Postgres. This repo can also run a userspace cluster on 55432.
ATABASE_URL=postgresql://psql:DevPsql@123@localhost:5432/content_review

# System Postgres after: sudo -u postgres createuser -s "$USER" && sudo -u postgres createdb content_review
# DATABASE_URL=postgresql://localhost:5432/content_review

